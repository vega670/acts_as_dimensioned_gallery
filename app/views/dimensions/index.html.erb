<h1>Dimensions</h1>
<% @dimensions.each do |dimension| %>
  <div class="dimension">
    Name: <%= link_to dimension.name, dimension %><br />
    Height: <%= dimension.height %><br />
    Width: <%= dimension.width %><br />
    Aspect: <%= dimension.aspect  %><br />
    Crop: <%= dimension.crop %><br />
    Resize: <%= dimension.resize %><br />
    Used By:
      <% dimension.galleries.each do |gallery| %>
        <%= link_to gallery.name, gallery %>
      <% end %>
    <br />

    <%= link_to 'Edit', edit_dimension_path(dimension) %> |
    <%= link_to 'Delete', dimension, :method => :delete, :confirm => 'Are you sure?' %>
  </div>
  <br />
<% end %>
<%= link_to 'New Dimension', new_dimension_path  %>
<%= @holder.class.name %>
<%= Work.automatic_polymorphic_paths %>
<%= url_for([@holder, 'dimensions']) %>
<%= dimensions_path %>
<%= request.path.split('/')[1] <=> 'dimensions' %>