<% @galleries.each do |gallery| %>
	<div class="gallery">
		Name: <%= gallery.name %><br />
		Description: <%= gallery.description %><br />
		
		<%= link_to 'Manage dimensions' %>
	</div>
<% end %>
<%= @holder.class.name %>